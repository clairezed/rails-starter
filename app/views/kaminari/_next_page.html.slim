/Link to the "Next" page
/  - available local variables
/    url:           url to the next page
/    current_page:  a page object for the currently displayed page
/    total_pages:   total number of pages
/    per_page:      number of items to fetch per page
/    remote:        data-remote

- content_for :next_page do
  span=> t('views.pagination.next').html_safe
  i.fa.fa-arrow-right>

/ span.next.btn.btn-light class="#{'disabled' if current_page.last?}"
/   == link_to_unless current_page.last?, content_for(:next_page), url, rel: 'next', remote: remote

span.next
  = link_to_unless current_page.last?, content_for(:next_page), url, rel: 'next', remote: remote, class: "btn btn-secondary mr-1"

/ span.prev
/   = link_to_unless current_page.first?, content_for(:prev_page), url, rel: 'prev', remote: remote, class: "btn btn-secondary mr-1"